//Problem 2: Greet Each Person in an Array

const names = ["John","Blake","Taylor","Ben","Cidney"];

for (let i = 0; i < names.length; i++){ // i keeps incrementing until it reaches length of array
    console.log(`Hello, ${names[i]}!`);
}